/**
 * Entry Adapter Infrastructure
 *
 * 导出所有 entry adapter 相关的类型和工具
 */

export type {
  EntryAdapter,
  EntryContext,
  AdapterStatus,
  AdapterConfig,
} from './types';

export { adapterRegistry } from './registry';
